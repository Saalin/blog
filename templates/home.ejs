<h1 style="padding-bottom: 10px"><%- title %></h1>

<ul>
    <% for(let i=0; i<posts.length; i++) {%>
        <li>[ <%= posts[i].rawDate %> ] <a href="<%= posts[i].url %>"><%= posts[i].title %></a></li>
     <% } %>
</ul>